Run this cmd to setup env vars
```bash
export DATABASE_URL="host=localhost user=gorm password=gorm dbname=gorm port=9920 sslmode=disable TimeZone=Asia/Shanghai" GO_APP_ENV="development" JWT_SECRET="SECRET"
```
